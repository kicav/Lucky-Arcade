{
  "name": "Lucky Arcade Laravel 13",
  "image": "mcr.microsoft.com/devcontainers/php:1-8.3",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Lucky Arcade",
      "onAutoForward": "notify",
      "protocol": "http"
    }
  },
  "postCreateCommand": "chmod +x run-codespaces.sh setup-linux.sh upgrade-v0.3.sh && php -v && composer --version && php -m | grep -Ei 'pdo_sqlite|sqlite3' || true",
  "remoteUser": "vscode"
}
